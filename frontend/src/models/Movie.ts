export type Movie = {
    id: string;
    title: string;
    releaseYear: string;
    genre: string;
    runtime: string;
    language: string;
    rating: number;
    isFavorite?: boolean;
    poster: string;
};
